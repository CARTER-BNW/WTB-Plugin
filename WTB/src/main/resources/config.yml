# Database configuration
# NOTE (L2): SQLite is suitable for small/medium servers.
# For high-traffic servers (50+ concurrent traders) switch to MySQL below.
mysql:
  enabled: false
  host: "localhost"
  port: 3306
  database: "wtb"
  username: "root"
  password: ""